# Daily Summary — 2026-03-05

## Key highlights
- GLM-5 deep_gemm clarification: `--moe-runner-backend deep_gemm` silently falls back to `cutlass` on SM120 (deep_gemm unsupported); `cutlass` is faster than `flashinfer_cutlass` for MTP workloads; `SGLANG_ENABLE_DEEP_GEMM=0 SGLANG_ENABLE_JIT_DEEPGEMM=0` recommended
- GLM-5 with deep_gemm+MTP: 20–30 tok/s faster for Eagle speculative vs flashinfer_cutlass; no benefit without speculative
- NCCL graph XML (+11% on Genoa) confirmed again; combined with cutlass MoE backend pushes to 98–105 tok/s on 8x RTX 6000 Genoa
- GLM-5 FP8 KV still broken: only bf16 KV works, limiting context to 200k shared pool across all requests; contrast with vLLM+DCP for Kimi which supports 3M+ context
- SGLang PRs #19948 (DeepGemm scale fix) and #19951 (latest SGLang broken — crash fix) submitted by Festr
- Model load speed: SGLang `--model-loader-extra-config '{"enable_multithread_load": true, "num_threads": 8}'` achieves near-NVMe max throughput; vLLM stuck at ~4 GB/s vs llama.cpp's 11 GB/s; ZFS `arc_max=352GB` for RAM-preloading trick shared
- AlexGS discovers chat template misconfiguration was breaking Qwen3.5 instruction-following — fixing explicit chat template made Qwen work "as well as GLM"
- MiniMax M2.5 NVFP4: vLLM reported faster than SGLang for single/multi-batch; better TTFT at multi-batch
- Kimi K2.5 Eagle model exists; Festr blocked by no time to investigate; GLM-5 preferred over Kimi for agentic coding (trained specifically for it); vision workaround via claude-relay proxy routing images to Qwen
- Power supply / electrical discussion: 240V recommended over 120V for efficiency; 20A circuits minimum per 4x RTX 6000 Max-Q

## Channel activity
- **#glm-5**: Backend clarification (cutlass > flashinfer_cutlass for MTP); speed benchmarks; stability improving; Docker updated with all PRs
- **#general**: Model load speed benchmarks; ZFS RAM cache trick; power circuit planning for GPU rigs; SGLang vs vLLM for MiniMax M2.5
- **#qwen-35**: Chat template fix breakthrough; MTP 3-token instability (drop to 1–2 tokens recommended); Qwen3.5-27B NVFP4 on single card performance
