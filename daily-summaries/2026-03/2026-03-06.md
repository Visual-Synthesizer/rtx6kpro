# Daily Summary — 2026-03-06

## Key highlights
- GLM-5 NaN crash root cause found: `flashinfer_cutlass` FP4 GEMM backend has a race condition causing NaN values in probability tensor (`Assertion: probability tensor contains inf/nan/negative`); triggered when radix cache is hit concurrently
- Workaround confirmed: `--fp4-gemm-backend flashinfer_cudnn` avoids the crash; related FlashInfer issue #2708 opened; `--enable-nan-detection` prevents crash but doesn't fix root cause
- Crash reproduction script created: `test_cache_hit_concurrent.py --prefix-repeat 60 --concurrency 64` triggers crash reliably within minutes
- New Docker pushed: `voipmonitor/llm-pytorch-blackwell:nightly-fp4-prezero` with attempted fix; SGLang PR #19897 (radix cache + speculative = crash) identified as the core upstream issue
- GLM-5 crash persists without radix cache and without speculative decoding when using `flashinfer_cutlass` — points to `--fp4-gemm-backend` as the true culprit
- Claude Code + LSP integration guide shared: setting up LSP with Claude Code significantly improves file/function lookup speed vs grep-based search
- WRX90E-SAGE SE lockup investigation ongoing: chisleu's Asus board hard-locks (IPMI freezes too); memtest passed overnight; suspected PCIe fabric bug
- RTX 6000 Pro pricing: DRAM shortage cited; ASUS ESC8000A-E13 server 2x more expensive than 6 months ago; 2.2TB DDR5 purchased in June now reportedly 10x value
- Qwen3.5 122B NVFP4 on single card: works with vLLM, not SGLang; slower than llama.cpp for single-user workloads
- SGLANG_ENABLE_SPEC_V2=True required — without it, NEXTN falls back silently to non-v2 Eagle and loads model twice causing OOM

## Channel activity
- **#glm-5**: NaN crash root cause isolated to flashinfer_cutlass FP4 GEMM race condition; cudnn backend as workaround; extensive collaborative debugging session with Claude Code
- **#hardware**: RTX 6000 Max-Q WRX90E lockup troubleshooting; PCIe slot configuration; RAM price spike; NVIDIA RTX Pro Server station announced
- **#general**: Claude Code + LSP setup; Qwen3.5 122B single-card status; ZFS confirmed as freeze cause for some users
- **#qwen-35**: Benchmark comparisons; Qwen3.5 397B vs MiniMax M2.5 intelligence parity discussion
