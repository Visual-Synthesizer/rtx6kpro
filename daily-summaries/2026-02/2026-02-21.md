# Daily Summary — 2026-02-21

## Key highlights
- Root cause of Festr's 2-socket slowdown identified: AMD EPYC dual-CPU PCIe P2P must cross xGMI/IF interconnect — NCCL sees it as SYS-hop, adding latency; Grimulkan's PCIe switch topology avoids this
- With Grimulkan's NCCL env vars applied to Festr's docker: 53 → 77 tok/s on same hardware/command
- luke explains the "snake" PCIe ring topology and why NCCL defaults to a sub-optimal tree on AMD multi-socket; NCCL_P2P_LEVEL=SYS forces better ring routing
- P2P latency matrix shared: cross-socket pairs (e.g. GPU3↔GPU4) are same latency as intra-socket in p2pBandwidthLatencyTest, but collective bandwidth suffers heavily under real workloads
- DeepEP hybrid-EP branch shared as potential PCIe-optimized all-to-all collective: https://github.com/deepseek-ai/DeepEP/tree/hybrid-ep
- Festr published venv build script (voipmonitor.org/build_vllm_venv.sh) for reproducible CUDA 13.0 / torch 2.10 / vLLM environment with SM120 patches
- Venv vs docker performance delta confirmed at ~3–5 tok/s in favor of compiled venv
- Supermicro white paper on single-root vs dual-root GPU server topology shared; recommendation to keep GPU workloads on single NUMA node

## Channel activity
- **#general**: Extensive PCIe topology deep-dive; NCCL tuning; venv build script; multi-GPU comfy/video generation discussion
- **#hardware**: PCIe switch "snake" topology diagram; ACS/IOMMU settings affecting P2P; dual-CPU vs switch tradeoffs
