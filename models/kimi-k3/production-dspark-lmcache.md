# Kimi-K3 Production Runtime

The qualified Kimi-K3 service uses native vLLM host KV offload rather than
LMCache. The canonical deployment, Docker, benchmark, and rebuild specification
is [Kimi-K3 MXFP4 Runtime](production-runtime.md).

The filename is retained as a compatibility alias; it does not identify the
selected cache backend.
